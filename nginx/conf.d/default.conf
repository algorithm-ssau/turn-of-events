server {
    listen 80;

    # location / {
    #     proxy_pass http://service1:5555;  # Замените service1 и port на имя и порт вашего первого сервиса
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # location /service2 {
    #     proxy_pass http://service2:4444;  # Замените service2 и port на имя и порт вашего второго сервиса
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $scheme;
    # }

    # Добавьте дополнительные блоки location для других сервисов по мере необходимости
}